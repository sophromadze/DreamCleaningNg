// Temporary kill switch for the Card on File feature (2026-07-17).
// Set to true to unhide every card-on-file surface at once:
// booking save-card checkbox, saved-card payment option (booking-confirmation
// + order-payment), the profile Saved Card section, and the admin Charge button.
// No other code changes are needed to re-enable.
export const CARD_ON_FILE_ENABLED = false;
